{
  "version": 1,
  "integrations": [
    {
      "key": "calendly_booking",
      "name": "Calendly booking",
      "secrets": [
        {
          "key": "CALENDLY_API_TOKEN",
          "label": "Calendly API token",
          "helpText": "Token used to read availability and create scheduled invitees.",
          "required": true,
          "environments": ["local", "preview", "production"]
        },
        {
          "key": "CALENDLY_WEBHOOK_SIGNING_KEY",
          "label": "Calendly webhook signing key",
          "helpText": "Signing key used to verify Calendly invitee lifecycle webhooks.",
          "required": true,
          "environments": ["local", "preview", "production"]
        }
      ]
    }
  ]
}
